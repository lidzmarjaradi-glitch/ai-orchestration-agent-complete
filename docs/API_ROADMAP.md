# API Roadmap

This tested MVP is frontend-only for reliability. For production, add backend routes:

- POST /api/planner
- POST /api/evaluator
- POST /api/projects
- POST /api/iterations

Never expose model API keys in the browser.
